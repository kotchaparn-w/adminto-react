export type BackdropOption = {
    name: string;
    scroll: boolean;
    backdrop: boolean;
};

export type PlacementOption = {
    name: string;
    placement?: 'start' | 'end' | 'top' | 'bottom';
};
